PORT=5000
MONGO_URI=mongodb+srv://<DB_USER>:<DB_PASS>@cluster.mongodb.net/<DB_NAME>?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_long_random_value
ADMIN_USER=admin
# Do NOT put plaintext passwords here for production. Use a hashed user in the database instead.
# ADMIN_PASS is only used by the legacy fallback for local development.
# For production, create an Admin record using the provided create-admin script.
PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/safevoice?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
